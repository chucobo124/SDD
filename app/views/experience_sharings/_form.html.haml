= simple_form_for @experience_sharing , :html => { :class => 'form-inline' } do |f|
  = f.error_notification
  .inputs
    = f.input :ES_date
    = f.input :ES_context, as: :text
  .actions
    = f.submit 'Save'
